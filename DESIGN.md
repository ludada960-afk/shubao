# Design System

## Intent

薯包的工作台是一个面向商业图片生产的专业工具。视觉系统以高可读的中性画布、稳定的白色工具表面和有限的品牌蓝为核心。最具识别度的元素不是装饰，而是把白底图、主图、详情图、SKU 和素材按业务用途组织成横向资产轨道。

## Color

| Role | Value | Use |
| --- | --- | --- |
| Canvas | `#F7F8FA` | 无限画布和工作区底色 |
| Surface | `#FFFFFF` | 工具栏、菜单、节点底色 |
| Ink | `#15171A` | 标题、主要命令和高优先级信息 |
| Muted ink | `#626871` | 次要信息、尺寸和用途 |
| Hairline | `#E3E6EA` | 分隔、默认节点边界 |
| Command blue | `#2563EB` | 选择、端口、主命令、键盘焦点 |
| Brand coral | `#E8544B` | 品牌识别和少量强调，不用于普通选择 |
| Success | `#27864B` | 已保存、完成、在线 |
| Warning | `#B76A00` | 可恢复异常和需要确认 |
| Danger | `#C43D35` | 删除和不可继续错误 |

强调色只表达状态和命令。画布不能被单一紫色、蓝色或暖米色占据，也不使用渐变文字、装饰光球或玻璃拟态。

## Typography

产品界面统一使用 `Inter, Noto Sans SC, PingFang SC, Microsoft YaHei, system-ui, sans-serif`。工具栏与数据采用 12-13px，节点标题 13-14px，页面标题 15-16px。字距为 `0`，不使用流式字号或展示字体。标题依靠字重和空间分层，不依靠夸张尺寸。

## Geometry

- 顶栏 52px，左右内容保持单行，窄屏时命令区独立横向滚动。
- 左侧添加栏宽 48px；底部主工具栏高 44px；左下缩放控制高 36px。
- 图片节点宽度默认 230px，媒体高度按真实比例计算，底部元数据区 54-64px。
- 工具表面圆角 8px；图片节点圆角 6-8px；胶囊仅用于状态和分段控制。
- 工具表面使用短而清晰的阴影，最大模糊 8px；有阴影时不叠加装饰性边框。

## Canvas Shell

顶栏只保留返回、项目名、保存状态和明确的保存/导出命令。左侧栏提供添加图片、从作品导入、电商套图和文本。底部中间提供选择、抓手、图片和文本，左下提供缩小、百分比、放大和适配。

空画布中央显示“双击添加商品素材”，并提供“上传商品图”“从作品导入”“生成电商套图”三个动作。说明必须面向任务，不描述快捷键、实现方式或功能清单。

## Nodes And Connections

- 默认节点保持安静；悬停抬升 1px，并让直接关系保持清晰、无关节点降至 45%。
- 选中节点使用 2px 品牌蓝描边和稳定控制点。顶部上下文工具栏与节点对齐，编辑器紧邻节点下方。
- 单击图片主体即可选中和拖动；双击打开高清预览；右键打开紧凑动作菜单。
- 连接线和节点位于同一世界变换层，拖动时同帧更新；默认不显示关系文字。
- 源商品位于左侧；白底图、主图、详情图、SKU、素材各占一条横向轨道。每张图显示买家可理解的标题、比例和像素尺寸。

## Interaction States

所有按钮和图标命令必须具备默认、悬停、键盘焦点、按下、禁用和忙碌状态。常规反馈持续 150-220ms，使用 ease-out；减少动态模式下取消位移和光带，只保留即时颜色或透明度变化。生成进度显示在目标节点内部，不创建第二个悬浮进度窗口。

## Responsive Behavior

桌面保持完整画布外壳。宽度低于 720px 时，顶栏命令和底部工具可独立滚动，左侧栏缩为图标，节点与世界坐标不重新挤压。页面宽度始终等于视口宽度；画布内部平移和缩放不计入文档溢出。

## Image Quality

原图是唯一事实源。卡片根据容器和 DPR 使用 320/640/960/1600 的 AVIF/WebP 候选，详情预览预取并解码高分辨率候选。占位尺寸必须固定，禁止把低分辨率缩略图拉伸到更大的显示尺寸。
