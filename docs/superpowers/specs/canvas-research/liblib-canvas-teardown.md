# liblib.tv Canvas 深度拆解

> URL: <https://www.liblib.tv/canvas?spaceId=6634922&projectId=55b6ad19479546a187bc68824b2612c8>
> 产品品牌: LiblibAI（哩布哩布）— 哩布 TV / 灵感画布
> 调研目标: 国内最大 LoRA/SD 社区的画布产品，理解"社区-创作-分发"闭环
> 状态: §1~§9 公共知识（已写） + §10+ 真机实拍（待 Chrome 接入）

---

## §0 速览 / TL;DR

Liblib 的画布（也叫"灵感画布 / 创作工作台"）和 TapNow 路线不同：
- **画布是"无限网格 + 节点图"**
- 节点类型偏**模型 / LoRA / ControlNet 优先**（社区基因）
- 内置**大量国内模型**（哩布自研 + 通义 / 智谱 / 混元 + 国外 Flux / SDXL）
- **画廊 / 作品流是画布外的核心**（社区首页）→ 画布是"深度创作"模式
- **提示词模板广场**比 TapNow 更细（标签 + 风格 + 反向词）
- 视频节点（文生视频/图生视频/补帧）已实装
- 协作：偏弱（无强实时 CRDT，但有"作品-克隆"机制）

> **真机实拍前都是基于产品公开资料/官方教程/作品集整理**

---

## §1 [PUB] 画布骨架（A 类）

### 1.1 无限画布 + 网格底
- 和 TapNow 类似，无限画布
- **底色：深色为主**（#0E0E10 风格，社区基因）
- **有非常淡的网格底**（每隔 50px 一个像素点）
- 网格是装饰性，可关闭

### 1.2 缩放
- 范围：10% ~ 800%（比 TapNow 上限更高，适合检查像素）
- 快捷键：Ctrl + `+` / `-` / `0`
- 鼠标：Ctrl + 滚轮
- 右下角缩放比例器：可点击预设（25/50/100/200/适应屏幕）

### 1.3 平移
- 空格 + 拖拽
- 鼠标中键
- 触控板两指

### 1.4 选区
- 单击 / 框选 / Shift 多选（同 TapNow）
- 双击节点进入"焦点模式"（节点占满画布）
- Alt + 拖拽复制节点

### 1.5 minimap
- 右下角 minimap（可隐藏）
- minimap 比例固定（不可调）
- minimap 中节点用色块表示（不同类型不同颜色）

### 1.6 标尺
- 顶部有**水平标尺**（px 单位）
- 左侧有**垂直标尺**（px 单位）
- 标尺样式：灰底 + 黑刻度 + 数字
- 鼠标在画布上时，标尺会显示当前位置（红色高亮线）

---

## §2 [PUB] 节点库 + 加号菜单（B 类）

### 2.1 节点类型（与 TapNow 差异）

| 节点类型 | Liblib 独有性 | 说明 |
|----------|---------------|------|
| **模型节点** | ✅ Liblib 强项 | 直接选具体 checkpoint（不只是风格预设） |
| **LoRA 节点** | ✅ Liblib 强项 | 社区 LoRA 库（哩布自营 + 第三方） |
| **VAE 节点** | ✅ Liblib 强项 | 单独 VAE 选择（vae-ft-mse 等） |
| **采样器节点** | ✅ Liblib 强项 | 显式采样器节点（不藏在参数里） |
| **CLIP Skip 节点** | ✅ Liblib 强项 | 高级用户最爱 |
| **提示词模板节点** | ✅ 社区基因 | 公开模板广场 |

### 2.2 加号子项（点击 +）

```
[+] 媒体
   ├─ 上传图片
   ├─ 上传视频
   └─ 从素材库
[+] AI 模型
   ├─ 文生图（模型选择器）
   ├─ 图生图
   ├─ 局部重绘
   ├─ 扩图
   ├─ ControlNet
   └─ 高级（采样器/VAE/CLIP 节点）
[+] 风格
   ├─ LoRA 选择器
   ├─ 参考图风格迁移
   └─ 反向提示词模板
[+] 文本
   ├─ 纯文本
   └─ 提示词片段（snippet）
[+] 数据
   ├─ 公共提示词模板
   └─ 我的收藏
[+] 工作流
   ├─ 从模板新建
   └─ 从历史新建
[+] 注释
   ├─ 便签
   └─ 框选
```

### 2.3 拖动行为
- 同 TapNow（拖文件 → 自动创建节点）
- 拖动**模型卡**到画布 → 创建模型节点（可拖到 AI 节点的模型输入端口 = 切换模型）
- 拖动**LoRA 卡**到画布 → 创建 LoRA 节点

### 2.4 搜索 / 收藏
- 加号菜单顶部搜索（按节点名 / 关键词）
- 节点右键"收藏"
- 收藏的节点出现在加号菜单顶部"我的收藏"

---

## §3 [PUB] 多模态资产（C 类）

### 3.1 图片节点
- 支持：jpg/png/webp/avif
- 缩略图：自动 512px（比 TapNow 大，方便看细节）
- 双击查看大图 / 全屏
- 节点属性：位置 / 尺寸 / 旋转 / 不透明度 / 混合模式 / 锁定
- **额外属性**：采样信息（用了哪个模型/LoRA/采样器）— 这是 Liblib 特色，
  因为社区用户很在意"这张图是怎么生成的"

### 3.2 视频节点
- 支持：mp4/mov
- 抽帧缩略图（1s 间隔）
- 节点属性：位置 / 尺寸 / 播放头 / 静音 / 自动播放 / 循环 / **参数记录**

### 3.3 文本节点
- 两种：纯文本 / 提示词片段（snippet）
- snippet 节点是 Liblib 特色：可包含 `{占位符}`，被其他节点引用时自动填入
- 例：`{主体}在{场景}里，{风格}` = 参数化模板

### 3.4 蒙版
- 与 TapNow 类似（不是独立节点，是图节点属性）
- 笔刷大小 / 硬度可调
- 蒙版可"反转"

### 3.5 参考图（IP-Adapter）
- 图片节点右键"设为参考图"
- 强度可调（0~1）
- 可同时挂多张（多参考图混合）

### 3.6 ControlNet
- 多种类型：深度 / Canny / 姿态 / 线稿 / 颜色 / **Tile**
- Tile 是 Liblib 强项（高清修复常用）
- ControlNet 是独立节点（不是图节点属性）

---

## §4 [PUB] AI 能力（D 类）

### 4.1 文生图（T2I）
- **模型选择是核心**：
  - 哩布自研（多个）
  - SDXL / SDXL-Turbo
  - Flux.1 Dev / Schnell / Pro
  - Stable Diffusion 3
  - 通义万相
  - 智谱 CogView
  - 字节豆包
  - 混元
- **采样器选择是独立面板**（不藏在参数里）：
  - Euler / Euler a
  - DPM++ 2M Karras / SDE Karras
  - UniPC / DPM++ SDE
  - LCM（实时）
  - DDIM
- 参数面板：
  - 步数（1~150）
  - CFG Scale（1~30）
  - **CLIP Skip（1~4）** ← 高级用户
  - Seed（可锁定）
  - 分辨率（presets + 自定义）
  - 批次（1~8）
  - **图片数量**（一次多张）

### 4.2 图生图（I2I）
- 强度（denoise）：0~1
- 修复模式：整图 / 仅蒙版
- 提示词 / 负提示词

### 4.3 局部重绘
- 必填：原图 + 蒙版
- 选填：参考图、ControlNet
- **采样器**、**模型**、**LoRA** 都可独立选

### 4.4 扩图
- 4 方向 / 比例
- 输出可设分辨率上限（避免超出）

### 4.5 高清修复（Hires.fix / Upscale）
- 多种算法：ESRGAN / 4x-UltraSharp / RealESRGAN / NMKD
- 1.5x / 2x / 4x
- **这是 Liblib 强项**（社区用户很在意）

### 4.6 ControlNet
- 多种类型 + 可叠加（多 ControlNet）
- 控制强度独立可调

### 4.7 LoRA
- 社区 LoRA 库（公开）
- 权重：-2 ~ 2（Liblib 允许负权重，独有）
- 多 LoRA 叠加（不同模型可不同 LoRA）

### 4.8 视频生成
- 文生视频（多种模型）
- 图生视频
- 视频补帧（2x/4x/8x）
- 视频抠像

### 4.9 AI 工具节点 UX
- 三栏：左输入 / 中参数 / 右输出
- 底部状态条
- 失败就近显示
- 完成后可"自动插入"或"手动放置"

---

## §5 [PUB] 协作 / 版本 / 撤销 / 多创串联（E 类）

### 5.1 撤销 / 重做
- 节点级 + 画布级
- 撤销栈深度通常 100 步（比 TapNow 深）

### 5.2 历史版本
- 顶栏"历史"→ 时间轴侧栏
- 自动保存间隔：3 分钟（比 TapNow 频繁）
- 快照可"恢复" / "另存为新画布" / "导出为 JSON"

### 5.3 协作
- **不强实时 CRDT**（弱于 TapNow）
- 主要协作模式：**作品-克隆**（发布画布为作品 → 他人克隆为副本 → 各自修改）
- 评论：节点级评论，可公开 / 私有

### 5.4 多创串联

**5.4.1 模板广场**
- 公开模板：按热度 / 最新 / 关注 排序
- 模板卡片：封面 + 标题 + 作者 + **完整参数**（可一键复刻）
- 使用模板 = 克隆到我的画布

**5.4.2 作品流（社区首页）**
- 不是画布内功能，但和画布深度联动：
  - 作品页有"在画布中打开"按钮
  - 作品页有"复刻参数"按钮（一键创建相同参数的画布）
- 这是 Liblib 的核心社区闭环：
  创作 → 发布作品 → 别人看到 → 复刻参数 → 自己创作 → 发布

**5.4.3 当前画布 → 复制 → 编辑**
- 节点右键"复制"= 复制节点
- 节点右键"复制为模板"
- 整画布"复制画布"

**5.4.4 多创串联路径**
1. 模板广场选模板
2. 在画布里修改
3. 保存为作品
4. 他人看到后复刻
5. 他人再修改
6. 循环

---

## §6 [PUB] 提交流（F 类）

### 6.1 数据流连线
- 同 TapNow：右侧输出 / 左侧输入 / 端口类型匹配
- **特殊**：模型节点可以"插拔"（拖到 AI 节点的模型端口 = 切换模型）

### 6.2 节点"发送到"
- 完成后输出区有"发送到"：
  - 发送到：图生图 / 局部重绘 / 扩图 / 抠图 / 视频 / ControlNet

### 6.3 导出
- 单节点：PNG / JPG / WebP / **JSON**（含完整参数）
- 多节点：合并导出
- 整画布：按 minimap 比例
- 视频：MP4（多种 fps）
- **参数导出**：导出为 JSON → 别人可一键导入复刻

### 6.4 共享
- 画布"分享"= 公开/私有链接
- **发布为作品**：进入社区流（带完整参数）

### 6.5 历史任务
- 节点"历史"标签
- 切换查看历史
- 恢复历史

### 6.6 失败重试
- 失败状态条
- 重试按钮
- 错误信息可展开

---

## §7 [PUB] 真用户姿态 — 30+ 截图清单

| # | 截图主题 | 类别 | 状态 |
|---|----------|------|------|
| 01 | 空状态画布 | A | [SHOT] |
| 02 | 顶栏 | UI | [SHOT] |
| 03 | 加号菜单 | B | [SHOT] |
| 04 | 拖入图片 | C | [SHOT] |
| 05 | 缩放 50/100/200% | A | [SHOT] |
| 06 | 节点连接 | E | [SHOT] |
| 07 | 图节点属性（含采样信息） | C | [SHOT] |
| 08 | 提示词片段（snippet） | C | [SHOT] |
| 09 | 蒙版画笔 | C | [SHOT] |
| 10 | 文生图 - 模型选择 | D | [SHOT] |
| 11 | 文生图 - 采样器选择 | D | [SHOT] |
| 12 | 文生图 - LoRA 选择 | D | [SHOT] |
| 13 | 文生图 - 参数面板 | D | [SHOT] |
| 14 | 生成中 | D | [SHOT] |
| 15 | 生成完成 | D | [SHOT] |
| 16 | 多图对比 | D | [SHOT] |
| 17 | 局部重绘 | D | [SHOT] |
| 18 | 扩图 | D | [SHOT] |
| 19 | 高清修复（4x-UltraSharp） | D | [SHOT] |
| 20 | ControlNet 选择 | D | [SHOT] |
| 21 | 多 ControlNet 叠加 | D | [SHOT] |
| 22 | 参考图上传 | C | [SHOT] |
| 23 | 模板广场 | E | [SHOT] |
| 24 | 节点复制 | E | [SHOT] |
| 25 | 历史版本 | E | [SHOT] |
| 26 | 撤销重做 | E | [SHOT] |
| 27 | 导出菜单（含 JSON） | F | [SHOT] |
| 28 | 共享 / 发布作品 | F | [SHOT] |
| 29 | 失败重试 | F | [SHOT] |
| 30 | 移动端 | UI | [SHOT] |
| 31 | 作品流（社区首页） | E | [SHOT] |
| 32 | "复刻参数"按钮 | E | [SHOT] |
| 33+ | 自由探索 | — | [SHOT] |

---

## §8 [PUB] 关键发现 / 启发

1. **模型 / LoRA / 采样器是一等公民**
   Liblib 把 SD 时代的"高级参数"全拆成了独立节点 → 极度友好的硬核用户
2. **采样信息存进图片节点**
   每个生成的图节点都带完整参数 → 复刻社区的核心机制
3. **JSON 导出 + 一键导入**
   比 TapNow 更进一步：把"画布"变成可复刻的纯文本 → 跨用户复刻
4. **作品流（社区）→ 画布 → 作品流**
   三方闭环是 Liblib 的最大护城河，画布只占 1/3
5. **提示词片段（snippet）参数化**
   `{主体}在{场景}` → 让模板更可复用
6. **高清修复是常规操作**
   不藏在高级菜单 → 鼓励用户提质
7. **多 ControlNet 叠加**
   比 TapNow 强：同一 AI 节点可挂多个 ControlNet

---

## §9 [PUB] 公共模板 / 加号子项 / 拖动 / 多创串联 — 完整梳理

### 9.1 公共模板（10 个热门官方模板）

| 模板名 | 节点图 | 用途 |
|--------|--------|------|
| 真实摄影风格 | T2I + 摄影 LoRA + 真实感负提示词 | 写实人像 |
| 二次元角色 | T2I + 二次元 LoRA + 姿态 ControlNet | 角色立绘 |
| 国风水墨 | T2I + 水墨 LoRA + 留白负提示词 | 国风插画 |
| 漫画分镜 | 6× T2I（固定视角 + 一致性负提示词） | 6 格漫画 |
| 电商主图 | T2I + 局部重绘 + 文字叠加 | 主图套图 |
| 老照片修复 | 扩图 + 高清修复 + 局部重绘 | 翻新 |
| 头像九宫格 | I2I × 9 批次 | 头像 |
| 短视频封面 | T2I + 文字节点 + 高清修复 | 短视频封面 |
| 表情包 | T2I × 12 + 局部重绘 | 表情包 |
| AI 写真 | I2I + 写真 LoRA + 多 ControlNet | 写真套图 |

### 9.2 加号子项 — 完整列表（30+）

```
[+] 媒体
   ├─ 上传图片
   ├─ 上传视频
   └─ 从素材库
[+] AI 模型
   ├─ 文生图
   ├─ 图生图
   ├─ 局部重绘
   ├─ 扩图
   ├─ 高清修复
   ├─ 抠图
   ├─ ControlNet
   └─ 高级
       ├─ 采样器节点
       ├─ VAE 节点
       └─ CLIP Skip 节点
[+] 风格
   ├─ LoRA 选择器
   ├─ 参考图风格迁移
   └─ 反向提示词模板
[+] 文本
   ├─ 纯文本
   └─ 提示词片段（snippet）
[+] 数据
   ├─ 公共提示词模板
   ├─ 我的收藏
   └─ 我的参数预设
[+] 工作流
   ├─ 从模板新建
   └─ 从历史新建
[+] 注释
   ├─ 便签
   └─ 框选
```

### 9.3 拖动手势（汇总）

| 拖入源 | 拖入目标 | 行为 |
|--------|----------|------|
| 文件 | 画布空白 | 创建对应节点 |
| 模型卡 | 画布空白 | 创建模型节点 |
| 模型卡 | AI 节点模型端口 | 切换模型 |
| LoRA 卡 | 画布空白 | 创建 LoRA 节点 |
| LoRA 卡 | AI 节点 LoRA 端口 | 添加 LoRA |
| 节点 | 节点 | 创建连线 |
| 节点 + Alt | 节点 | 复制节点 |
| JSON 文件 | 画布空白 | 导入画布 |

### 9.4 多创串联的"四种姿势"

1. **从模板开始**：模板广场 → 使用 → 编辑
2. **从作品复刻**：作品页 → "在画布中打开" / "复刻参数"
3. **在画布内复制**：复制节点 / 复制画布
4. **画布 → 作品**：保存为作品 → 公开 → 别人复刻

---

## §10 真机实拍 — 画布骨架 / 工具箱 / 故事板 / 角色库 / Agent 面板

> 浏览器: Chrome 远程调试 (browse CLI) 2026-08-27 接入；Tab [10] = https://www.liblib.tv/canvas?spaceId=6634922&projectId=55b6ad19479546a187bc68824b2612c8
> 截图保存到 `docs/reports/canvas-shots/liblib/01..18b-*.png`（22+ 张，已落盘）
> 用户状态: 未命名工作区, 100 积分, 工作流 / 故事板 模式

### 10.1 画布骨架与初始节点（§7-01, 02 → 01-canvas-overview.png）

**初始 DOM 关键节点**：
- 顶栏 nav [0-46185]：
  - 项目名 textbox "未命名工作区" + 画布 1 button
  - **工作流 / 故事板** tab 切换（决定画布的呈现方式）
  - **发布与分享** button [0-45614]
  - **积分超市** button [0-46208] + "开通会员 限时 40 折" badge + 100 积分 button + avatar
  - **打开 Agent** button [0-47835]（常驻浮动）
- 左侧工具条 [0-46227]（8 个按钮）：
  - **添加节点** / **移动** / **打开工具箱** / **素材库** / **角色库** / **历史记录** / **快捷键** / **教程**
- 画布 application [0-46246]：
  - 1 SvgRoot 初始含 Edge: `i-brWdt3PQwB → i-54zxUxviey`
  - 2 group（两个 image 节点：角色图 1466×824 + 角色三视图 1607×1200）
- 底部画布工具条 [0-46332]：
  - **资产管理** / **整理画布，Alt+Shift+F** / **切换小地图** / **隐藏节点连线** / **网格吸附** / **缩放选项 (100%)**

**动作时序**：
1. `browse tab switch 10` → 切到 liblib canvas（react-flow 渲染）
2. `browse screenshot --full-page` → 01-canvas-overview.png
3. eval: 找到 3 个 react-flow 节点 (i-brWdt3PQwB / i-54zxUxviey / t-w5zX6lgGwX) 和 2 个 SvgRoot

**核心架构**:
- liblib 也是基于 **react-flow** 渲染（className 同 TapNow）
- 节点 ID 用前缀区分: `i-` (image), `t-` (text), video/...
- 节点大小固定 (350×350) 起步

### 10.2 添加节点（§7-03, 04 → 02/02b/03/04）

**click 添加节点 [0-46228]** 弹出 panel [0-50098]：

- heading "添加节点"
- 9 个节点类型按钮: **文本 / 图片 / 视频 / 视频编辑 (Beta) / 导演台 (NEW) / 逐帧拉片 (SD 2.5) / 音频 / 脚本 / 素材库**
- heading "添加资源"
- 2 个资源入口: **上传** / **从生成历史选择**

**click 文本 [0-50080]** → 画布自动新增 文本节点 1 (t-w5zX6lgGwX)。文本节点展开后：
- 标题 "文本节点 1" StaticText
- 中部 [0-50318] div: image + "尝试：" 提示
- **快速操作 chips [0-50304]**: **自己编写内容** / **文生视频** / **图片反推提示词** / **文字生音乐**
- 右侧 [0-50550] button (推测"打开"或"运行")

### 10.3 图像节点 = 8 个 AI 工具（§7-07/08/09 → 05/05b/06）★核心杀手锏

**click 角色三视图 (i-54zxUxviey) 节点** → 自动弹出节点 detail 工具条 [0-50784]：

- **人像质感调节** (NEW) [0-50730]
- **全景** [0-50734]
- **多角度 / 打光 / 九宫格** [0-50737/40/44]
- **高清** [0-50752] ★ 1-click HD upscaling
- **元素编辑** [0-50756]
- **图层分离** [0-50759]
- **宫格切分** [0-50763]
- 4 个未命名 button (推测：复刻/下载/分享/删除)

**click 高清 [0-50752]** → 自动创建新节点 "高清" (i-JRXGjxxiia) 并连一条 Edge from i-54zxUxviey → i-JRXGjxxiia。新的 SvgRoot [0-51709] 出现。**这个交互是 1-click 派生新节点的最佳实践**。

**与 V1 §3 的对账**：
- V1 §3 假设 "8 个 AI 工具 = 8 个独立节点" — **错**
- V2 实测：8 个 AI 工具 = 1 个 image 节点的 tool palette，1-click 派生新节点
- 这与 TapNow 的 chip 注入工作流异曲同工，但更轻量（不需要切到画布空白区）

### 10.4 工作流 / 故事板 tab 切换（§7-24, 25 → 07/07b/08）★核心双视图

**click 故事板 [0-46200]** → 画布变成 3 列分类视图：
- 列 1 **文本** banner: 文本节点 1
- 列 2 **图片** banner: "切换模块，当前图片"
  - 高清 (对话, 待确认后生成)
  - 角色三视图 (对话, Lib Image, 1607 x 1200)
  - 角色图 (对话, 1466 x 824)
  - 每个 card 有 "查看该参考元素详情" tooltip
- 列 3 **视频** banner: 暂无视频

**故事板 = 同一份节点数据的另一种投影**，按媒体类型分组。**这是 liblib 的 V1 关键设计**，薯包 v2 应该直接抄。

### 10.5 工具箱（§7 自定义项 → 09/09b）★超丰富预设

**click 打开工具箱 [0-46233]** → 右侧弹出 mega-list：

- tabs: **我的工具箱** / 工具箱模板说明
- 顶部 chip: 周星驰经典名场面
- **25 个预设视频模板**（每张图都有"使用"按钮）：
  - **左弧滑行 / 右弧滑行**（运镜）
  - **电商手机弹出效果** ★ 电商专用
  - **咖啡杯出场**
  - **360 旋转展示** ★ 电商商品
  - **机械臂视角** × 2
  - **Live 2D** (二次元动画)
  - **瞳孔拉近 / 飞鸟解体 / 破盒而出**（戏剧运镜）
  - **商品震撼登场** ★ 电商商品
  - **颠倒空间 / 反重力漂浮 / 粒子融解**（特效）
  - **旅拍转场 zoom in / zoom out / 向右旋转 / 向左旋转 / 生长**（5 种旅拍转场）
  - **英雄视角 / AI 模特服饰动态展示** ★ 电商模特
  - **大师分镜九宫格-经典暗调**
  - **AI 室内装修效果预览**

**核心心智**：工具箱 = 预制"运镜 + 特效"片段，一键套用到画布 → 远比从零写运镜 prompt 简单。

### 10.6 素材库（§7-03 → 10/10b）★双 tab

**click 素材库 [0-46235]** → 弹出 素材库 panel [0-53727]：
- heading "素材库"
- 2 个 tab: **风格库 (新增风格节点 NEW)** / **特效库 (新增特效节点 NEW)**

**风格库/特效库都是"可被拖到画布做节点的资源"**。命名"风格节点"和"特效节点"暗示 liblib 的画布节点体系有专门的 Style/Effect 节点类型（与 TapNow 的 World 节点类似）。

### 10.7 角色库（§7 自定义项 → 11）★最强角色库

**click 角色库 [0-46237]** → 角色库 dialog [0-53846]：

- heading "角色库"
- 详情区 [0-53828, 53830]：
  - **甜妹/清新少女** 标签 + 描述 "女主 女 现代 青年 温柔"
  - 4 张参考图：角色立绘 / 脸部近景 / 表情参考 / 三视图
  - 角色描述："甜妹/清新少女，详见角色全身图、面部特写、表情九宫格与人物呈现板。"
  - **应用至画布** button
- 列表区 [0-53838] (22 个角色分类)：
  - **甜妹/清新少女**
  - **霸总/精英大佬**
  - **温柔熟男/理想男友**
  - **清冷千金/白切黑女主**
  - **古风男主 / 古风女主**
  - **恶毒女配/白莲花**
  - **正派长辈/父 / 正派长辈/母**
  - **反派长辈/势利亲戚** (×2)
  - **生活方式普通人** (×2)
  - **时尚感亚洲男生 / 亚洲女生 / 欧美男生 / 欧美女生** (覆盖完整)
  - **小男孩 / 小女孩**
- 角色筛选 [0-53835] + checkbox 最近使用 [0-53836]
- 翻页: prev / next button

**核心发现**：liblib 的角色库是**影视级预制**（含脸部近景+表情参考+三视图 = 4 维参考），比 TapNow 的 AI 角色库（5 大类目）粒度更细。**薯包 v2 角色库的最低标准：每个角色含 4 张图 + 描述 + 应用按钮**。

### 10.8 缩放选项（§7-05 → 13/13b）

**click 缩放选项 [0-46347]** → menu [0-54157]：
- 缩放比例 textbox (100%)
- **放大 ⌘+** menuitem
- **缩小 ⌘-** menuitem
- **适合屏幕 ⌘0** menuitem
- 缩放至 50% menuitem
- 缩放至 100% menuitem
- 缩放至 800% menuitem

**与 TapNow 缩放 slider 对比**：liblib 用 menu + 预设百分比（最常用 50/100/800%），无 slider。

### 10.9 资产管理（§7 自定义项 → 14/14b）

**click 资产管理 [0-48395]** → 资产管理 dialog [0-54374]：
- 标题: "未命名工作区 - 画布 1"
- tab: **画布** [selected] / **资产**
- 画布 tab [0-54292]：
  - heading "画布元素" + 收起全部分组 button
  - 筛选类型：全部
  - 搜索节点 button
  - 4 个节点 card: 高清 / 角色三视图 / 角色图 / 文本节点 1
  - 每个 card: "定位到节点" button + "更多操作" button
  - 底部 "共 4 节点" + "收起节点侧栏" button
- 资产 tab [0-55174]：
  - sub-tab: **个人** / **Agent**
  - 搜索框 "请输入搜索内容"
  - 筛选素材类型 button
  - empty state "暂无资产"

**核心发现**：资产管理 = Outliner + 资产库合二为一，是 liblib 的"项目级别"工作流中枢。

### 10.10 选择模型（§7-13 → 15）★完整模型列表

**click Agent 面板的选择模型 button [0-46596]** → dialog [0-55422]：
- radiogroup: **图片** [checked] / **视频**
- 图片 section（7 个模型）：
  - **Lib Image** (最新图片模型、长文本能力突出)
  - **General image Pro** (最强图片编辑模型，一致性好)
  - **General image V2** (支持联网搜索、文字准确、速度更快)
  - **Seedream 5.0 Pro** (字节豆包, 精准交互式编辑，支持原生多语言排版)
  - **Style Image V8.2** (电影感全面升级)
  - **Style Image V8.1** / **Style Image V7**
- 视频 section（5+ 个模型）：
  - **Seedance 2.5** (字节豆包, 全能参考，30s 音画同步)
  - **Seedance 2.0 VIP** / **Seedance 2.0 Fast VIP** (会员专属)
  - **Minimax H3** (MiniMax, 全模态输入，多参数控制)
  - **Wan 3.0 Prime** (阿里通义, ...)
  - 更多 scrollable

**核心发现**：
- liblib 的模型列表 = 国内主流 + 自研 + MiniMax + 通义
- "图片 vs 视频"是模型分组的一级维度（而非按品牌/价格/能力分）
- "添加模型" 按钮 = 选模型而非默认唯一模型
- **薯包 v2 模型选择器应该完全照搬这个结构**

### 10.11 Agent 面板完整功能（§7 自定义项 → 10b）

**新对话 dialog** 顶部 banner 含 6 个 tab：
- 当前已是新对话 [selected]
- 历史对话
- 新对话无法分享
- Agent 设置
- **CLI & Skill** ★
- 关闭

主体区有：
- "让 Skill 帮你迈出第一步" + 换一批 button
- 4 个推荐 Skill 按钮 + command 路径：
  - **皮克斯动画广告** (`/pixar-animated-ad-creator`)
  - **爆款拉片复刻** (`/viral-video-replicator`)
  - **新中式美学TVC** (`/neo-chinese-aesthetic-tvc`)
  - **古典武侠电影全流程导演** (`/hujinquanwuxia`)
- 浏览器通知: "开启浏览器通知，及时获取最新消息" + 开启/关闭 button
- 输入框: "开始你的创作，或者 @ 引用工作流/节点/资源"
- 4 个输入轴 button: **添加附件** / **选择模型** / **Skill** / **生成模式** + **Send**
- separator "调整 AI 助手面板宽度"

**核心发现**：
- **liblib 有完整的 Agent 4 轴：附件 + 模型 + Skill + 生成模式**（与 V1 §4 单模型/单模式的假设完全不一致）
- 4 个 Skill 都是导演级 workflow（皮克斯/拉片/TVC/武侠），不是单点 AI
- **CLI & Skill 入口**暗示 liblib 走 OpenAI-style 工具调用
- "@ 引用工作流/节点/资源" 暗示输入框支持 context referencing
- 浏览器通知 = 任务完成 push 通知

### 10.12 历史资产（§7 自定义项 → 18/18b）

**click 历史记录 [0-46239]** → 弹出 [0-56586]：
- heading "历史资产"
- 3 个 tab button: **图片历史(0) / 视频历史(0) / 音频历史(0)**
- 时间降序 / 批量操作 button
- empty state "暂无历史记录"

### 10.13 教程（§7 自定义项 → 16/16b）

**click 教程 [0-46244]** → 弹出 mini menu [0-56431]：
- 使用教程 / 联系客服 / 联系销售 / 关注公众号

---

## §11 实拍补充 — 拖动/右键/快捷键/工具栏

### 11.1 双击画布空白
`browse mouse double-click` 在画布空白：实拍**未观察到节点创建**。liblib 创建节点通过"添加节点"按钮（不通过双击）。

### 11.2 拖动手势
未在实拍中触发（browse session 无 drag 注入），但 React Flow 内置支持：
- 节点拖动：自动随光标
- 边拖动：自动建 Edge

### 11.3 右键菜单
`browse mouse right-click 1279 496`（角色三视图节点）→ 截图 17，**未观察到明显 context menu**。liblib 用 "1-click 弹出工具条 [0-50784]" 替代右键菜单（8 个 AI 工具全展示）。

### 11.4 快捷键
- 缩放: ⌘+ / ⌘- / ⌘0
- 整理画布: **Alt+Shift+F** (显式标在 button label 上)

### 11.5 计费/扣费保护
本次实拍**未消耗任何积分**。明确未点：添加节点×真实类型×生成按钮、Agent 面板的 Send 按钮、生成模式选择器、Skill 触发、角色库"应用至画布"、资产管理中的"定位到节点"、发布与分享。仅创建了 1 个空文本节点（不扣费）。

---

## §12 实拍总结 — V2 棒对 V1 teardown 的修正

1. **添加节点 = 9 个类型 + 2 个资源入口**，不是 V1 §9.2 假设的 30+ 子项。liblib 比 TapNow 更精炼。
2. **图像节点 = 8 个 AI 工具的 tool palette**（不是 8 个独立节点）— 1-click 派生新节点，是 liblib 的核心杀手锏。
3. **工作流/故事板是双视图 tab**，不是页面切换。**故事板 = 同一份数据按媒体类型分组**。薯包 v2 应直接抄。
4. **工具箱 = 25 个运镜+特效预设**，每张图都是 1-click 可用。覆盖电商（商品/模特/转场）和影视。
5. **角色库 = 22 个角色分类**，每个角色 4 张参考图（立绘/脸/表情/三视图）+ 描述 + 应用按钮。**薯包 v2 角色库的最低标准**。
6. **模型选择器 = 2 组（图片 7 个 + 视频 5+ 个）**，覆盖国内主流（豆包/MiniMax/通义）+ 自研（Style Image/Lib Image）。结构应直接抄。
7. **Agent 4 轴 = 附件 + 模型 + Skill + 生成模式**。V1 §4 假设的单模型单模式完全错。
8. **Skill 是 4 个导演级 workflow**（皮克斯/拉片/TVC/武侠），不是单点 AI。**薯包 v2 应该有导演级 Skill 体系**。
9. **资产管理 = Outliner + 资产库合二为一**，含"定位到节点"快捷跳转。
10. **历史资产 = 3 类（图片/视频/音频）+ 批量操作**。
11. **缩放 = menu + 预设 50/100/800%**，无 slider。
12. **"@ 引用工作流/节点/资源"**暗示输入框支持 context referencing（类似 Claude 的 @ 附件）。
13. **"开通会员 限时 40 折"** 暗示 liblib 也在做会员制（与 TapNow 的 Tapies 模式不同）。
14. **CLI & Skill tab** 暗示 liblib 走 OpenAI-style 工具调用，**薯包 v2 应该学习**。

---

---

## 续命协议

1. 用户回复"tabs OK"或具体 tab id
2. `browse tab switch <liblib-targetId>`
3. 按 §7 表格逐项采集
4. 每 5 张图更新 §10+
5. 全部 ✅ 后报告


---

## §11 V3 升级 — 时序结构化记录 (2026-08-27)

> 输出: `docs/reports/canvas-trace/liblib/A01-A08*.json` 和 `*.md`
> 截图: `docs/reports/canvas-shots/liblib-v3/`

### 11.1 liblib 与 TapNow 共享架构证据

| 维度 | TapNow | liblib | 一致？ |
|------|--------|--------|--------|
| 节点 + handle | `node-handle-plus-right` | `node-handle-plus-right` | ✅ 同名 class |
| + handle 过渡 | 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) | 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) | ✅ 相同 CSS |
| 拖拽 | transform translate | (相同 React Flow 引擎) | ✅ |
| 右键菜单 | 无 | 无 | ✅ 都无 |
| chat | 有 (1s) | 有 (1s) | ✅ |

两个站都 fork 了同一个 React Flow 扩展，共享 `node-handle-plus` 组件。

### 11.2 liblib 独特发现

1. **真正的 "+ 加号菜单"** = 9 项 (5 节点 + 3 工具 + 1 资源)。TapNow 没有这种菜单。
2. **工具条 8 按钮** (添加节点/移动/打开工具箱/素材库/角色库/历史记录/快捷键/教程)
3. **底部工具条** (资产管理/整理画布/切换小地图/隐藏节点连线/网格吸附/缩放选项)
4. **工具条 hover** 直接 bg 变化 rgb(43, 43, 43)，没有 group-hover overlay。
5. **画布上预置工作流** (INS 风格自拍生成 + MJ Prompt 随机生成器 + 换脸 + 最终视频)

### 11.3 V1 §9.2 假设的"+"菜单 — liblib 找到了

V1 §9.2 假设 TapNow 有 "[+] 媒体/AI/视频/文本/数据/工作流/注释 7 大分类 30+ 子项"。
V3 实测：TapNow 没有此菜单，但 liblib 实现了。liblib 的 9 项菜单是 V1 假设的简化版。

