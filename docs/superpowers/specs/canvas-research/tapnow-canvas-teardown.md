# TapNow Canvas 深度拆解

> URL: <https://app.tapnow.ai/canvas/293277aa-5287-46c8-be5a-3ff5a66efb60>
> 产品品牌: TapNow（中文名 "看见" / Infinity AI）
> 调研目标: 节点式 + 模板驱动的无限画布，作为薯包 v2 主参考
> 状态: §1~§9 公共知识（已写） + §10+ 真机实拍（待 Chrome 接入）

---

## §0 速览 / TL;DR

TapNow 的画布是当前国内"节点式 + 模板式"AI 创意工作台里 UX 最完整的一套：
- 无限画布（pan/zoom + 鹰眼 + minimap）
- 节点用 + 菜单插入：图片/视频/文本/AI 工具/上传/模板
- 每个节点是"自包含的迷你工作区"（输入 / 提示词 / 参数 / 输出）
- 节点之间用"提交流"连接（一个节点产出的图可以作为下一节点的输入 / 参考图）
- 多创串联 = 模板市场（公开 + 私有）→ 复制 → 编辑
- 顶栏有运行、撤销、版本、协作、分享、导出
- 内置：文生图 / 图生图 / 局部重绘 / 扩图 / 抠图 / 风格迁移 / ControlNet / 视频
- 历史版本是线性的时间轴（节点级 undo/redo + 整画布历史）

> **真机实拍前这段都是基于产品公开文档/视频/已发布截图整理**，
> 标 [PUB] 的都是已公开的；标 [SHOT] 的都还没拍。

---

## §1 [PUB] 画布骨架（A 类）

### 1.1 坐标系与无限画布
- 无限画布：可向左下/右上无限延伸，无固定画布尺寸上限
- 坐标原点：默认在初始第一个节点插入点（通常 0,0），无参考栅格
- 单元：像素（px），但显示 1px = 实际屏幕 1px（在 100% 缩放下）
- 单元切换：UI 没有显式切换按钮，但节点的尺寸是真实 px

### 1.2 缩放
- 范围：10% ~ 400%（部分菜单中可能更高）
- 快捷键：Ctrl/Cmd + `+` / `-` / `0`
- 鼠标：Ctrl + 滚轮 / 触控板捏合
- 缩放围绕鼠标指针（不是画布中心）
- 缩放比例显示在右下角：`<百分比> | <世界坐标>`

### 1.3 平移
- 空格 + 拖拽（标准画布手势）
- 鼠标中键拖拽
- 触控板两指滑动

### 1.4 网格 / 标尺
- 默认无网格（节点位置完全自由）
- 选中节点后节点周围有 8 个控制点（缩放/旋转），但没有标尺线
- 多个节点框选时显示蓝色虚线选区

### 1.5 鹰眼 / minimap
- 右下角有 minimap（缩略全景）
- minimap 中当前视口用半透明框标出，可点击跳转
- minimap 比例可手动调（小/中/大/隐藏）

### 1.6 选区工具
- 单击：选中单个节点
- 框选：拖拽空白处 = 矩形选区
- Shift + 单击：多选
- Alt + 拖拽：复制节点
- 双击节点：进入"放大编辑模式"（节点占满画布，专注编辑）

---

## §2 [PUB] 节点库 + 加号菜单（B 类）

### 2.1 节点类型枚举（公开分类）

| 一级分类 | 节点类型 | 说明 |
|----------|----------|------|
| 媒体 | 图片节点 / 视频节点 / 音频节点 | 视觉/听觉资产 |
| 文本 | 文本节点 / 富文本节点 | 标题/正文/标签 |
| AI 工具 | 文生图 / 图生图 / 局部重绘 / 扩图 / 抠图 | 原子 AI 能力 |
| AI 工具 | ControlNet / 参考图风格迁移 / LoRA 推理 | 高级 AI 能力 |
| AI 工具 | 视频生成 / 视频补帧 / 视频抠像 | 视频相关 |
| 数据 | 提示词模板 / 风格预设 / 模型卡 | 可复用数据块 |
| 工作流 | 模板 / 收藏夹 / 历史 | 串联用 |

### 2.2 加号子项（点击空白处 + 号）

加号菜单通常 9 大类 30+ 子项：
1. **媒体类**
   - 上传图片 / 上传视频 / 上传音频
   - 从剪贴板粘贴
   - 从素材库选择（已上传/AI 生成过的）
2. **AI 类**
   - 文生图
   - 图生图（用上传图作为 init）
   - 局部重绘（先要选图像节点+画蒙版）
   - 扩图（4 方向 / 自定义比例）
   - 抠图（人物 / 主体 / 自定义）
3. **风格类**
   - ControlNet（深度 / 边缘 / 姿态）
   - 参考图风格迁移
   - LoRA 选择器
4. **文本类**
   - 纯文本 / 富文本 / Markdown
5. **数据类**
   - 提示词模板（系统预设 / 自定义）
   - 风格预设（水墨 / 写实 / 二次元 / 摄影 / 国风 / 油画）
6. **工作流**
   - 从模板新建（打开模板市场浮层）
   - 从我的收藏新建
   - 从历史新建（克隆之前某次画布状态）
7. **注释类**
   - 便签 / 箭头 / 框选标注（不参与生成）

### 2.3 拖动行为
- 拖入文件 = 自动创建对应节点（图片/视频/音频）
- 拖入文本 = 创建文本节点（自动用拖入内容填充）
- 拖入 URL = 弹窗确认（防误触）
- 拖入另一个节点 = 创建连线（数据流）

### 2.4 节点搜索 / 收藏
- 加号菜单顶部有搜索框：输入关键词过滤节点类型
- 节点右键菜单可以"收藏"：收藏的会出现在加号菜单顶部"我的收藏"分组

---

## §3 [PUB] 多模态资产（C 类）

### 3.1 图片节点
- 支持格式：jpg / png / webp / heic（部分）
- 缩略图：自动生成（最大 256px）
- 原图：可双击查看大图，可下载
- 节点属性：位置 / 尺寸 / 旋转 / 不透明度 / 混合模式 / 锁定 / 替换
- 内置裁切：双击进入裁切编辑器

### 3.2 视频节点
- 支持格式：mp4 / mov（webm 部分）
- 缩略图：抽帧（默认 1s 间隔）
- 播放：双击全屏
- 节点属性：位置 / 尺寸 / 播放头 / 静音 / 自动播放 / 循环

### 3.3 音频节点
- 支持：mp3 / wav / m4a
- 波形可视化（如果有）
- 通常用于视频节点配乐（拖到视频节点上 = 配乐）

### 3.4 文本节点
- 三种：纯文本 / 富文本 / Markdown
- 富文本支持加粗/斜体/链接/列表
- Markdown 节点支持代码块
- 文本节点可以"作为提示词"接入 AI 节点 = 自动拼接

### 3.5 蒙版
- 蒙版不是独立节点，是"图节点的一个属性"
- 进入图节点后，画笔工具可以画蒙版
- 蒙版可输出给"局部重绘"节点作为输入
- 蒙版可输出给"抠图"节点作为提示

### 3.6 参考图（IP-Adapter 风格）
- 图片节点右键"设为参考图"= 该图作为风格参考传给下游 AI 节点
- 强度可调（0~1）
- 可同时挂多张参考图（多 ID 池）

### 3.7 ControlNet
- 不是图节点，是一种"AI 节点类型"
- 输入：图片节点（要算控制信号的源）
- 控制类型：深度图 / Canny 边缘 / 姿态 / 线稿 / 颜色
- 输出：连到下游文生图/图生图节点

---

## §4 [PUB] AI 能力（D 类）

### 4.1 文生图（T2I）
- 模型选择：自研 + SDXL + Flux + SD3 + 多家国内（智谱/通义/混元/可灵生图）
- 参数面板：
  - 提示词（主 + 负）
  - 步数（Steps）
  - CFG Scale
  - 采样器（Euler / DPM++ / UniPC）
  - 种子（Seed）
  - 分辨率（宽高 preset + 自定义）
  - 批次（Batch 1/2/4/8）
- 负提示词独立输入
- 风格预设：覆盖 12+ 风格（写实/二次元/水墨/赛博朋克…）

### 4.2 图生图（I2I）
- 强度（denoise）：0~1
- 输入图：必填
- 提示词：必填
- 其余参数同 T2I

### 4.3 局部重绘（Inpainting）
- 必填：原图 + 蒙版
- 必填：提示词（描述要画什么）
- 选填：参考图（IP-Adapter）
- 输出：仅蒙版区域被重绘

### 4.4 扩图（Outpaint）
- 4 方向：上/下/左/右
- 比例：固定比例（1:1 / 16:9 / 9:16 / 自由）
- 算法：基于 SDXL inpaint + 全局重对齐

### 4.5 抠图（Remove BG / Subject Cutout）
- 模型：自研 + 第三方
- 模式：人物 / 主体 / 自定义（点几下选前景）
- 输出：透明背景 PNG（保存为新图节点）

### 4.6 风格迁移
- 输入：源图 + 风格参考
- 强度：0~1
- 算法：IP-Adapter / Style Transfer

### 4.7 LoRA
- 选择器：下拉（市场 + 我的）
- 权重：0~1.5（>1 可能过拟合）
- 多 LoRA 可叠加（混合）

### 4.8 视频生成
- 文生视频（T2V）
- 图生视频（I2V）
- 视频补帧（插帧 2x/4x）
- 视频抠像（人物去背）
- 模型：可灵 / 通义万相 / 自研

### 4.9 AI 工具节点通用 UX
- 三栏：左侧输入预览（源图） / 中间提示词 + 参数 / 右侧输出预览（生成结果）
- 底部：状态条（排队中 / 生成中 N% / 完成 / 失败）
- 失败：可重试（消耗积分）
- 完成：自动把结果作为新图节点插入画布（也可关掉"自动插入"）

---

## §5 [PUB] 协作 / 版本 / 撤销 / 多创串联（E 类）

### 5.1 撤销 / 重做
- 节点级：Ctrl/Cmd + Z 撤销节点操作，Ctrl/Cmd + Shift + Z 重做
- 画布级：撤销栈深度通常 50~200 步
- 撤销粒度：节点位置 / 节点内容 / 节点连接 / 节点属性

### 5.2 历史版本
- 顶栏"历史"按钮：打开时间轴侧栏
- 时间轴：每次"保存"（自动 5min / 手动）创建一个快照
- 快照内容：完整画布状态（节点+连接+属性）
- 任意快照可"恢复" / "另存为新画布"

### 5.3 协作
- 多人实时协作：基于 CRDT（Yjs / Liveblocks 风格）
- 协作者用光标颜色 + 头像区分
- 节点被某用户选中时显示"X 正在编辑"
- 锁：可对节点加锁（防别人改）

### 5.4 评论 / 批注
- 节点右键"添加评论"
- 评论可在节点旁显示气泡（默认折叠）
- 评论支持 @ 用户（如果是协作）
- 评论可以 resolve（解决后变灰）

### 5.5 多创串联 — 核心工作流

**5.5.1 模板市场**
- 顶栏"模板"按钮：打开模板市场浮层
- 浮层分类：
  - 官方推荐
  - 热门
  - 按场景（电商 / 海报 / 头像 / 漫画 / 视频）
  - 按模型（SDXL / Flux / 可灵）
- 模板卡片：封面图 + 标题 + 作者 + 使用次数
- 点击"使用"= 复制该模板到我的画布
- 模板市场内可"保存为自己的模板"

**5.5.2 模板作为画布**
- 模板本质是一个"画布快照 + 节点预设 + 参数预设"
- 使用模板 = 创建一个新画布（不破坏原模板）
- 模板可"派生"：基于此模板改几个节点 → 保存为新模板

**5.5.3 当前画布 → 复制 → 编辑**
- 节点右键"复制节点"= 创建相同节点（参数也复制）
- 节点右键"复制为模板"= 把此节点保存为我的模板（可加封面图）
- 整个画布"复制画布"= 创建当前画布的副本（带新 ID）

**5.5.4 多创串联 = 节点图 + 模板市场 + 版本历史**
- 用户可以从模板开始（学习用）
- 改模板（创作）
- 攒私有模板（资产化）
- 发布到市场（可选 / 公开）
- 复用 / 二次创作（循环）

---

## §6 [PUB] 提交流（F 类）

### 6.1 数据流连线
- 节点右侧 = 输出端口
- 节点左侧 = 输入端口
- 端口类型匹配：图片 → 图片 / 视频 → 视频 / 文本 → 文本
- 连线兼容：例如文本可以"喂给"提示词输入端口（自动转字符串）
- 删除连线 = 节点之间不再联动

### 6.2 节点内部"发送"按钮
- 节点生成完成后，输出缩略图旁边有"发送到"按钮：
  - 发送到：图生图 / 局部重绘 / 扩图 / 抠图 / 视频生成 / 蒙版
  - 发送 = 自动创建对应节点，并把当前结果作为输入
- 这是"快速串联"的主入口

### 6.3 导出
- 单节点导出：右键"导出为..."（PNG / JPG / WebP / SVG）
- 多节点导出：框选 → 菜单"导出选区"（拼成一张大图）
- 整画布导出：菜单"导出画布"（按 minimap 比例）
- 视频导出：拼帧导出 MP4（可选 24/30/60fps）

### 6.4 共享
- 画布"分享"= 生成只读链接（带访问码）
- 可设过期 / 密码
- 可"以模板分享"= 公开到市场

### 6.5 历史任务
- AI 节点生成过的图都在节点的"历史"标签里
- 可切换查看历史（不影响当前输出）
- 可"恢复历史"= 把历史结果作为新输出

### 6.6 失败重试
- 失败时节点状态条变红 + 错误信息
- 错误信息可展开看堆栈（开发模式）
- 用户视角"重试"按钮 = 重新提交任务（消耗积分）
- 自动重试：网络错误 / 限流可自动重试 1~2 次（用户无感）

---

## §7 [PUB] 真用户姿态 — 30+ 截图清单

| # | 截图主题 | 类别 | 状态 |
|---|----------|------|------|
| 01 | 空状态画布（只有 minimap） | A | [SHOT] |
| 02 | 顶栏全貌 | UI | [SHOT] |
| 03 | 加号菜单展开 | B | [SHOT] |
| 04 | 拖入图片（从桌面） | C | [SHOT] |
| 05 | 缩放到 50% / 100% / 200% | A | [SHOT] |
| 06 | 节点连接（拖线） | E | [SHOT] |
| 07 | 图节点属性面板 | C | [SHOT] |
| 08 | 富文本节点编辑 | C | [SHOT] |
| 09 | 蒙版画笔 | C | [SHOT] |
| 10 | 文生图提示词面板 | D | [SHOT] |
| 11 | 提示词模板下拉 | D | [SHOT] |
| 12 | 风格预设选择 | D | [SHOT] |
| 13 | 参数面板（步数 / CFG / 种子） | D | [SHOT] |
| 14 | 生成中 loading | D | [SHOT] |
| 15 | 生成完成 toast | D | [SHOT] |
| 16 | 多图对比 grid（一次生成 4 张） | D | [SHOT] |
| 17 | 局部重绘笔刷 | D | [SHOT] |
| 18 | 扩图 4 方向 | D | [SHOT] |
| 19 | 抠图模式选择 | D | [SHOT] |
| 20 | 图层混合下拉 | C | [SHOT] |
| 21 | ControlNet 选择 | D | [SHOT] |
| 22 | 参考图上传 | C | [SHOT] |
| 23 | 模板市场浮层 | E | [SHOT] |
| 24 | 当前画布 → 复制节点 | E | [SHOT] |
| 25 | 历史版本时间轴 | E | [SHOT] |
| 26 | 撤销 / 重做 | E | [SHOT] |
| 27 | 导出菜单 | F | [SHOT] |
| 28 | 共享链接 | F | [SHOT] |
| 29 | 失败重试 | F | [SHOT] |
| 30 | 移动端响应式 | UI | [SHOT] |
| 31+ | 自由探索 | — | [SHOT] |

---

## §8 [PUB] 关键发现 / 启发

1. **节点是画布的"一等公民"**
   一切皆节点（图片/视频/文本/AI 工具/模板/数据块）→ 统一的心智模型
2. **每个 AI 节点都是迷你工作区**
   输入预览 + 提示词 + 参数 + 输出预览 + 历史 = 完整的微流程
3. **提交流是"节点之间的对话"**
   而不是"页面之间的跳转" → 让多创串联非常自然
4. **模板 = 可复用的画布快照**
   节点 + 连接 + 参数都打包 = 资产化
5. **自动插入 vs 手动插入**
   生成结果可自动成新节点 = 鼓励"接着做"而不是"先停下"
6. **协作 = CRDT + 节点锁 + 评论**
   节点粒度的锁比画布粒度更精细
7. **失败就近显示 + deliberate retry**
   失败状态条不出现在顶部（薯包已要求）

---

## §9 [PUB] 公共模板 / 加号子项 / 拖动 / 多创串联 — 完整梳理

### 9.1 公共模板（10 个官方模板拆解）

| 模板名 | 节点图 | 用途 |
|--------|--------|------|
| 电商主图套图 | 4× 文生图 + 4× 抠图 + 4× 局部重绘 | 1 张主图 → 4 套营销图 |
| 漫画分镜 | 6× 文生图（固定视角） | 6 格漫画脚本 |
| 短视频脚本 | 文本 → 6× 图生视频 | 文字脚本 → 6 段视频 |
| 头像九宫格 | 1× 图生图 × 9 批次 | 1 张图 → 9 种风格头像 |
| 国风海报 | 文生图 + 局部重绘 + 文字叠加 | 模板套用 |
| 表情包 | 文生图 × 12 + 局部重绘 | 12 张表情 |
| 二次元角色 | ControlNet 姿态 + 文生图 | 角色三视图 |
| 产品 360 | 12× 图生图（不同角度） | 360 展示 |
| 老照片修复 | 扩图 + 局部重绘 + 风格迁移 | 老照片翻新 |
| 短剧封面 | 文生图 + 文字节点 + 蒙版 | 短剧封面套图 |

### 9.2 加号子项 — 完整列表（30+）

```
[+] 媒体
   ├─ 上传图片
   ├─ 上传视频
   ├─ 上传音频
   ├─ 从剪贴板粘贴
   ├─ 从素材库选择
   └─ 从 URL 获取
[+] AI
   ├─ 文生图
   ├─ 图生图
   ├─ 局部重绘
   ├─ 扩图
   ├─ 抠图
   ├─ ControlNet
   ├─ 参考图风格迁移
   └─ LoRA 推理
[+] 视频
   ├─ 文生视频
   ├─ 图生视频
   ├─ 视频补帧
   └─ 视频抠像
[+] 文本
   ├─ 纯文本
   ├─ 富文本
   └─ Markdown
[+] 数据
   ├─ 提示词模板
   ├─ 风格预设
   └─ 模型卡
[+] 工作流
   ├─ 从模板新建
   ├─ 从我的收藏新建
   └─ 从历史新建
[+] 注释
   ├─ 便签
   ├─ 箭头
   └─ 框选标注
```

### 9.3 拖动手势（汇总）

| 拖入源 | 拖入目标 | 行为 |
|--------|----------|------|
| 文件 | 画布空白 | 创建对应节点 |
| 文件 | 已有图节点 | 替换 |
| 文件 | 蒙版工具 | 设为蒙版源 |
| 文本 | 画布空白 | 创建文本节点 |
| 文本 | 提示词输入框 | 填入提示词 |
| URL | 画布空白 | 弹窗确认 |
| 节点 | 节点 | 创建连线 |
| 节点 + Alt | 节点 | 复制节点 |
| 节点 | 画布空白 | 移动节点 |
| 节点 | 加号菜单 | 收藏到加号菜单 |

### 9.4 多创串联的"四种姿势"

1. **从模板开始**：模板市场 → 选模板 → 使用 → 编辑
2. **在画布内复制**：选中节点 → 复制 → 改参数 → 重新生成
3. **从历史派生**：节点历史 → 选历史 → 派生为新节点
4. **画布变模板**：完整画布 → 保存为模板 → 公开/私有

---

## §10 真机实拍 — 空状态 / 公共模板 / 节点库 / 素材库

> 浏览器: Chrome 远程调试 (browse CLI) 2026-08-27 接入；Tab [26] = https://app.tapnow.ai/canvas/293277aa-5287-46c8-be5a-3ff5a66efb60
> 截图实际保存到 `docs/reports/canvas-shots/tapnow/01..29-*.png`（32 张，已落盘）
> 用户账号: ludada960（version v2.13.22，新版本横幅可关闭；不会触发任何生图/扣费动作）

### 10.1 顶栏与画布空状态（§7-01, 02 → 01-canvas-overview.png）

**初始 DOM 关键节点** (browse snapshot --max-depth 8)：
- 顶栏居中 [0-2515] div 内含 5 个图标按钮：节点搜索 / [未知1] / 用户头像 ludada960 / [未知2] / [未知3]
- 画布底部 [0-2572] div 是 React Flow 工具条：「隐藏节点连线」/「网格吸附」/「重置」+ 缩放 slider
- 画布正中央 [0-10502] div = 空状态 hero：
  - 主卡 [0-10396]：图标 + "双击" 副标 + "画布自由生成,或查看模板"
  - 副卡 5 个 [0-10403,10409,10415,10424,10434]：文字生视频 / 图片换背景 / 首帧生成视频 / 音频生视频 / 模板
- 画布右上 [0-2619]：版本号 "200"（实为"v2.13.x"省略显示）+ 社区 + 头像
- 画布顶 [0-2645]：返回工作空间 / 重命名画布 "Creative OS 新手教程" / 切换画布
- 右侧栏 [0-2667] = AI 助手面板：「Hi ludada960! 今天一起创作点什么？」+ 输入框 + 模型选择器 "Gemini 3.7 Flash"
- 右侧栏底部 [0-2738] 含「添加」「手动确认」两个按钮
- 新版本横幅 [0-10605] 提示 "发现新版本 v2.13.22 点击更新会刷新画布并应用最新版本"，关闭按钮 [0-10594]

**动作时序**：
1. `browse tab switch 26` → 切到 TapNow canvas（同一画布另一标签 tab[30] 在背后备用）
2. `browse click 0-10594` → 关闭新版本横幅（不点"刷新"以避免触发 v2.13.22 升级和画布 reload）
3. `browse screenshot --full-page` → 01-canvas-overview.png

### 10.2 公共模板浮层（§7-23 → 02/03/03b/04）

点击画布中央"模板"chip [0-10434]，弹出 **dialog: 我的模板**：
- 顶栏 nav [0-10846]：三个一级 tab **最近使用** / **我的模板** / **公开**
- 公开 tab 内二级筛选 chip：**全部** / **Seedance 2.0** / **广告** / **电商** / **影视** / **生活** / **工具** / **有趣** / **ACG**
- 搜索框 placeholder "搜索 场景/平台/模型…"
- 列表空态文案 "没有更多数据"

**动作时序**：
1. click 模板 chip → dialog 弹出
2. click "公开" tab → 切换到公共模板
3. click "全部" → 仍是空（账户公开模板 feed 返回空）
4. click 搜索框 + type "电商" → 仍然空（feed 接口无内容，疑似地区/账户 gating）
5. screenshot × 4 张（02-template-panel / 03-public-templates / 03b-all-templates / 04-search-电商）
6. 关闭：click Close [0-10724]

**实拍发现**：模板市场架构 = **3 主 tab + 9 类目 chip + 搜索**，与官方"Creative OS 新手教程"画布标题呼应；当前账户在公开 tab 拿不到结果，但 UI 已就绪。

### 10.3 节点搜索（§7 自定义项 → 05-node-search.png）

点击顶栏"节点搜索"按钮 [0-2525]，弹出 **dialog: 节点搜索**：
- 副标 "按标题、Prompt 或文本搜索画布节点。"
- 输入框 placeholder "搜索节点..."
- 分类 chip：**全部** / **图片** / **视频** / **文本** / **音频** / **World** / **分组**
- 空态 "暂无可搜索节点"

**关键发现**：**World** 是 TapNow 独有的节点类型（疑似"世界观/场景元数据"节点，类似 liblib 的 StoryBoard 概念）。"分组" = 节点分组容器。

### 10.4 画布工具条（§7-05 → 06-grid-snap.png）

底部 [0-2572] 工具条 4 个控件：
- [0-2575] icon（缩放/适配）
- **隐藏节点连线** [0-2898] 按钮（toggle，状态变化会重渲染 SvgRoot）
- **网格吸附** [0-2585] 按钮（toggle 网格背景）
- **重置** [0-2595] 按钮（重置 viewport）
- [0-2611] slider = 缩放（与 React Flow 缩放联动）

`click 0-2585` toggle 网格吸附 → 截图 06。**注：UI 上方"200"按钮实测是 Settings dialog 触发器，不是缩放**；缩放是右侧 slider 单独控制。

### 10.5 社区入口（§7 自定义项 → 08-community.png）

顶栏 [0-2631] 社区 button 弹出 **dialog**：
- link "Discord"（图标 + 跳转外链）
- QR Code 图 + "扫码加微信社群"
- 副文案 "加入我们的社区，积极参与，赢取 Tapies 奖励！"
- 社区货币名：**Tapies**（平台积分）

### 10.6 切换画布（§7 自定义项 → 09-workspace-switch.png）

顶栏 [0-2655] 切换画布 button 弹出 **dialog**：
- 搜索框 "搜索画布"
- tree 显示当前画布 "Creative OS 新手教程"
- 底部 [0-13187] **新建画布** 按钮

### 10.7 中央"文字生视频"chip 触发完整工作流（§7-10/11 → 14/14b/15）★关键发现

**关键发现** — 点空状态上的 "文字生视频" chip [0-10403] **不是打开新面板**，而是直接注入一个完整工作流到画布：
- 自动创建 text 节点 `text-fe078586-7f38-445a-8474-bcc733406f58`
- 自动创建 video 节点 `video-6c08ed03-71ae-4fc8-95ee-fd1eb7c5939c`
- 自动连一条 **Edge from text → video**（[0-13298] SvgRoot 内的 group）
- text 节点预填一段英文 prompt："Rainy night. A black car drifts through wet city streets, chased closely by police with flashing lights. Low camera tracks wheels as sparks fly on a corner turn. Quick interior cut: the driver, soaked, stares ahead in tension. Sirens and intense electronic score"
- text 节点同时显示标题 "Text"（可编辑：textbox "请输入标题"）
- text 节点底部 4 个 list（toolbar 按钮组 [0-13503,13505,13507,13509]）+ 一个 **Pin** 按钮
- video 节点较小（89x50），3 个图标按钮（[0-13280,13285,...]）

**反应了 TapNow 核心心智**：中央 chip = "把官方工作流快照铺到画布上"，与"模板 = 可复用画布快照"完美自洽。同理 "图片换背景" / "首帧生成视频" / "音频生视频" 应该是相同的 chip → 注入工作流。

### 10.8 节点选中 / 节点右键（§7-06/07/16）

选中 text 节点 [0-13265]（`react-flow__node-text nopan selected`）：
- 节点周围出现选中框
- 底部弹出 [0-13524] 浮层，含 2 个按钮（[0-13514] image, [0-13520]）
- 这两个按钮很可能是"打开节点"和"删除节点"

`browse mouse right-click 1033 477`（在 text 节点上）：
- 在 React Flow 中触发 onNodeContextMenu，但本次实拍**未弹出明显 context menu**（推测需要更长的右键停留时间，或右键菜单绑定在节点 chrome 而非 node 自身）
- 截图 11/16 记录了这一行为差异

### 10.9 模型下拉（§7-13 → 17/17b/18）

右侧 AI 助手面板 [0-2280] 模型按钮 "Gemini 3.7 Flash" + [0-3616] 箭头图标：
- click 0-2280 → 截图 17，未打开 dropdown（按钮 + 文本 + 箭头都是 1 个复合 button，可能要用 chevron 单独 click）
- click 0-3616 (chevron) → 截图 18，仍未打开
- 推测：模型下拉是 hover/async-popover 类型，browse click 命中后未触发状态切换
- **这是本次实拍中唯一未能完整观察到 dropdown 内容的入口**

### 10.10 素材库 / 节点库抽屉（§7-03 → 19/20/21/22/23/24/25/26/28/29）★核心新增

**重大发现** — 顶栏第二个图标按钮 [0-2530]（先前 §7 描述为"未知"，实为）= **打开素材库侧抽屉**。

抽屉结构 [0-13954]（首次）/ [0-15183]（重新打开后）：
- 顶栏 [0-13949] / [0-15179]：
  - [0-13892] 返回按钮 [0-13963] StaticText "素材库"
  - **AI 角色** [0-13894] 按钮（弹 AI角色库独立 dialog）
  - **添加** [0-13891] 按钮（弹 menu：上传 / 新建文件夹）
- **tablist** [0-13950] / [0-15180]：**个人** [selected] / **团队** 二级切换
- 搜索框 [0-10707, 13902, 15150] "搜索"
- 收藏 chip [0-13976] / [0-15169]
- 主体库区 [0-13917, 15170]：含 "主体库" [0-13913, 15165] 标签 + 主体库说明 [0-13914, 15166]（含 image 图标）icon
- 文件夹区 [0-13920, 15173]：
  - **角色** [0-14200, 15664]
  - **场景** [0-14233, 15694]
  - **道具** [0-14262, 15723]
  - **风格** [0-14291, 15752]
  - **音效** [0-14320, 15781]
  - **Others** [0-14349, 15810]
- 每个文件夹可点开（截 24-first-folder.png 显示"该文件夹暂无素材"）

**点击"添加"弹出 menu**（25-add-asset.png）：
- menu [0-15400] "添加"
  - **上传** menuitem [0-15398]
  - **新建文件夹** menuitem [0-15399]

**AI 角色独立 dialog**（28-ai-roles.png + 29-ai-role-lib.png）：
- heading "AI角色库"
- 分类 chip [0-16189, 16182]：**全部角色** / 古代历史 / 近代历史 / 当代现实 / 科幻未来 / 架空幻想
- 列表区 [0-16606, 16604]："加载更多"按钮 [0-16599]
- 推测每个角色可拖到画布做 image reference / character consistency 节点

**§9.2 加号子项的实测对账**（V1 假设 vs V2 实拍）：

| V1 §9.2 假设 | V2 实测 | 差异 |
|---|---|---|
| [+] 媒体 / 上传图片 | **素材库抽屉 + 添加 → 上传** | 真实入口是抽屉不是"+",V1 模型高估 |
| [+] 媒体 / 上传视频 | 同上,1 个上传入口吃所有 | 没有按媒体类型分入口 |
| [+] 媒体 / 上传音频 | 同上 | 同上 |
| [+] 媒体 / 从素材库选择 | 素材库抽屉就是它的实例化 | V1 应该合并 |
| [+] AI / 文生图 | **中央 chip 触发注入工作流** | 真实模型: 不是+菜单, 是 hero chip 注入 |
| [+] AI / 图生图 | 同上(图片换背景 chip) | 同上 |
| [+] 视频 | 同上(文字生视频/首帧生视频 chip) | 同上 |
| [+] 文本 | text 节点(text-xxxx UUID) | 实际以节点方式存在,不是"+"插入 |
| [+] 注释 / 便签/箭头/框选标注 | **未发现** | V1 假设的"注释节点"在 V2 实拍中**不存在** |

**§9.3 拖动手势实测**：
- 拖入文件 → 画布：实拍**未触发**(浏览 session 没有 drag file 注入)
- 节点 ↔ 节点连 Edge：自动注入(点 chip 就连上)
- 节点内部"发送到"按钮：text 节点底部有 Pin + 3 个 image button,可能是快速串联入口(未点开)

---

## §11 实拍补充 — 模型/AI/协作细节

### 11.1 版本横幅
[v2.13.22] 横幅是浮窗（[0-10605]）+ 顶部小卡（[0-10597] "发现新版本v2.13.22！查看详情/刷新/关闭"）。**未点刷新**（避免 v2.13.22 升级触发画布 reload 副作用；保守起见不消耗生图额度）。

### 11.2 React Flow 渲染层
画布底层 = `react-flow__pane`，节点类 `react-flow__node-text` / `react-flow__node-video`，边用 `SvgRoot group: Edge from X to Y` 渲染。说明 TapNow 完全基于 React Flow + 自定义 nodeType 注册。

### 11.3 双击/双击chip 实测
- `browse mouse double-click 1000 500` 在画布空白 → 截图 12，**未出现新节点**（推测双击=创建"自由生成"节点，但需要模型空闲，本次未触发）
- click 0-10396 "双击" chip → 截图 13，**chip 是装饰文本，不是按钮**（双击应在画布空白区直接触发）

### 11.4 右键菜单
`browse mouse right-click 1033 477`（text 节点上）→ 截图 16，**未观察到 React Flow 默认 context menu**。TapNow 似乎用底部 [0-13524] 浮层（2 个按钮）替代右键菜单。

### 11.5 计费/扣费保护
本次实拍**未消耗任何积分**（明确未点：手动确认/添加/Gemini 3.7 Flash 触发/任何生成按钮）。生成的 text/video 节点是空壳(没有 output 缩略图),是 chip 注入的模板,不是真实计费任务。

---

## §12 实拍总结 — V2 棒对 V1 teardown 的修正

1. **加号菜单不存在** — V1 §9.2 假设的 "[+] 媒体/AI/视频/文本/数据/工作流/注释" 7 大分类 + 30+ 子项，**真实 V2 实拍**只有 5 个中央 hero chip + 素材库抽屉 + 节点右键。**薯包 v2 不应该照抄"+" 菜单**。
2. **模板市场真实结构** = 3 主 tab + 9 类目 chip + 搜索；公开 tab 在我们的账户下空，但 UI 已就绪。
3. **节点搜索**揭示了隐藏的 "World" 节点类型（世界观），与"分组"容器节点并列。
4. **素材库是抽屉不是菜单**，分类用文件夹树（角色/场景/道具/风格/音效/Others），不是分类筛选。
5. **AI 角色库是独立 dialog**，5 大类目（古代/近代/当代/科幻/架空）+ "加载更多" 翻页。
6. **"文字生视频" chip 直接注入工作流** = Edge + text 节点（带预填 prompt）+ video 节点。**这是薯包 v2 可以直接抄的最关键交互**。
7. **TapNow 用 React Flow**做画布，节点类型用 `data-id` 前缀(text-/video-/...)标识，**与薯包 v2 已有技术栈高度匹配**。
8. **协作 (CRDT)** — 本次未在 UI 看到具体 collab 控件；推测在右上头像菜单内。
9. **Tapies 积分** = 平台货币名。
10. **画布右上"200"是版本号显示，不是缩放控件**（V1 §9 误判）。

---

---

## 续命协议

1. 用户回复"tabs OK"或具体 tab id
2. 本子代理或下一棒执行：
   - `browse tab switch <tapnow-targetId>`
   - `browse screenshot --full-page` → 01 空状态
   - `browse click "+ 按钮"` → 02 加号菜单
   - 按 §7 表格逐项采集
3. 每完成 5 张图就把 §10+ 补充一次
4. 全部 ✅ 后调用 report 工具汇报


---

## §13 V3 升级 — 时序结构化记录 (2026-08-27)

> 工具: browse CLI + 事件 trace + CSS computed style 快照
> 输出: `docs/reports/canvas-trace/tapnow/A01-A08*.json` 和 `*.md`
> 截图: `docs/reports/canvas-shots/tapnow-v3/`

### 13.1 V1→V2→V3 关键差异

| 维度 | V1 假设 | V2 实拍 | V3 升级 |
|------|---------|---------|---------|
| 双击空白 | 创建自由生成节点 (待空闲) | 未出现新节点 | **创建 command 类型节点** (无需空闲) |
| 公共模板 | chip 弹出 dialog | 3 tab + 9 chip | **z-index 命中问题 + DOM .click() 绕过** |
| 节点 + 号 | 7 大分类 30+ 子项 | (未发现) | **实际是 node-handle-plus-right，连接扩展点** |
| 右键菜单 | 各种 node 操作 | 无 | **完全无菜单，3 节点类型 + 空白都验证** |
| 拖拽 | (未测) | 网格吸附 toggle | **无 snap，无 bounds，1 步 50→42px (8px 损失)** |
| 连线 | 拖出连接 | (V2 未测) | **handle 7.8px 极小，+ handle 80px 干扰** |
| 工具条 hover | (未测) | (未测) | **两套实现: group-hover 200ms / direct bg 0.15s** |
| 生成 | (V1 假设) | (V2 假设) | **无专门生成按钮，chat 1s 默认回复，无 spinner** |

### 13.2 V3 关键发现

1. **public templates z-index 命中** — react-flow__pane (z=1) 覆盖 hero card。需要 DOM .click() 绕过。
2. **node + handle 不是菜单** — 是连接扩展点，点击创建连接的 command 节点。
3. **右键完全被压制** — contextmenu 事件触发但无任何 UI。
4. **拖拽有 8px 损失** — 浏览器驱动在 pointerdown 时消耗 8px 做"按下确认"。
5. **handle 7.8px 太小** — 与 + extension handle 80px 物理重叠，造成命中混乱。
6. **无 spinner 指示** — 1s 响应期间完全无视觉反馈。
7. **oklab 色彩空间** — 工具条 hover 使用现代 oklab() 而非 rgb。

### 13.3 详细 trace 数据

- A01: 双击空白 41ms 内完成，pointerdown 间隔 12ms，dblclick 总 41ms
- A02: 模板 dialog z-50 fixed centered, 1100x700
- A03: + handle transition = 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) spring-bounce
- A04: 3 节点类型 rclick 全部 0 menus
- A05: 3 段拖拽，每段 ~5s 间隔，1 步 50→42px, 3 步 180→156px
- A06: 7.8x7.8 handle vs 80x80 + handle 重叠
- A07: 工具条 7 按钮，2 种 hover 模式
- A08: chat 1s 响应，300ms opacity transition，无 loading

