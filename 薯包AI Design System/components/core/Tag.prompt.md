**Xiaohongshu-style #hashtag chip.** Auto-prefixes `#` when given a plain string.

```jsx
<Tag>反向旅游</Tag>          // → #反向旅游
<Tag active>小众旅行地</Tag>
```

Use in wrap rows for note hashtags. Pass `active` for a selected/filled state, `onClick` to make it interactive.
