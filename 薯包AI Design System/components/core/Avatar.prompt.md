**Rounded creator/user avatar.** Falls back to a gradient initial when no image.

```jsx
<Avatar src="/me.jpg" name="小薯" size={44} ring />
<Avatar name="薯包" />
```

Use `ring` to mark the active/own user. Size is the diameter in px.
